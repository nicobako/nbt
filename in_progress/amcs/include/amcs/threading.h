#ifndef AMCS_THREADING_H_
#define AMCS_THREADING_H_

#include <amcs/threading/Thread.h>
#include <amcs/threading/Mutex.h>

namespace amcs {

	/** \brief Conains tools for multithreading support.
	 *  
	 *  \todo Add an operating system (OS) independant layer to
	 *        to abstract OS-dependant function calls.
	 *
	 *  The amcs::threading namespace contains classes
	 *  and functions that make it easier to create multithreaded applications.
	 */
	namespace threading {

	}
}

#endif
